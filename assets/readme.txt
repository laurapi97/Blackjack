  //REVISA STRINGS Y SI ES A VALE 11, SI ES OTRA LETRA 10
// SI EL VALOR QUE EXTRAE NO ES UN STRING LO MULTIPLICA POR 1 PARA PASARLO A FORMATO NUMERO

    // console.log({valor});

    // let puntos = 0;


    // se llama a la funcion is Not a Number para identificar si es un numero o no
    // if (isNaN(valor)){

    //     puntos = (valor ==='A') ? 11: 10; // REVISA STRINGS Y SI ES A VALE 11, SI ES OTRA LETRA 10
    //     // regresa un valor booleano

    //     // console.log('No es un número');
    // } else{
    //     // console.log('Es un número');
    //     // puntos = valor; // devuelve un string cuando es > a 0
    //     puntos = valor* 1;// lo convierte a número
    // }

    // console.log(puntos); // cuando es morado es un numero y cuando es gris un string ( lo que se muestra en consola)

  
  

// const valor = valorCarta(pedirCarta()); // llama la función de la carta que se extrae y muestra el valor
// console.log({valor});

//REVISA STRINGS Y SI ES A VALE 11, SI ES OTRA LETRA 10



// const valor = carta[0]; // los strings pueden ser trabajados como arreglos
    // extrae parte de una cadena de texto y la devuelve como una nueva sin modificar a la original

  // el carta.length eliminta el último caracter de la cadena
// RESUMIENDO EL CÓDIGO DE ABAJO